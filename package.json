{
  "scripts": {
    "slides": "./convert.sh",
    "index": "node generate-index.js",
    "build": "npm run slides && npm run index"
},
  "devDependencies": {
    "@marp-team/marp-cli": "latest"
  }
}
